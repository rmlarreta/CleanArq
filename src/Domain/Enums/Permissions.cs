namespace Domain.Enums
{
    public enum Permissions
    {
        // ----------------------------------------------------
        // Roles
        // ----------------------------------------------------
        RoleList = 1,
        RoleAdd = 2,
        RoleEdit = 3,
        RoleDelete = 4,
        RoleViewDetail = 5,

        // ----------------------------------------------------
        // User
        // ----------------------------------------------------
        UserChangeRole = 10,
    }
}
